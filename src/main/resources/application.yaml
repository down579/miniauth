spring:
  datasource:
    url: "jdbc:mysql://localhost:3306/mini_auth?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC&characterEncoding=UTF-8"
    username: "root"
    password: "!@tngkr304"
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
    properties:
      hibernate:
        jdbc:
          time_zone: UTC

server:
  servlet:
    session:
      timeout: 30m
      cookie:
        http-only: true
        same-site: lax
