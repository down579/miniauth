spring:
  datasource:
    url: "jdbc:mysql://localhost:3306/mini_auth?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC&characterEncoding=UTF-8"
    username: "root"
    password: "!@tngkr304"
  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false
    properties:
      hibernate:
        jdbc:
          time_zone: UTC

app:
  jwt:
    secret: ${JWT_SECRET:change-me-to-a-long-random-secret-key-32bytes}
    expiration-ms: 900000
